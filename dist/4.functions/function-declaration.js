"use strict";
function holidaySale(product, sales, tax, discount) { }
function holidaySale1(product, sales, tax, discount) {
    let x = 10;
    return x;
}
function holidaySale11(product, sales, tax, discount) {
    let price = 0;
    let x = 1;
    let z = "1";
    let y = "str";
    if (y === "str")
        console.log("values are same");
    return price;
}
const test = holidaySale1("sunglasses", 10, 0.15, 0.1);
console.log(test);
