let n: number = 10;
// below is the code to create the arrow function
let x1 = (x: number, y: number, z: string): number => {
  let a: number = 10;
  x = x + y;
  z = z + 1;
  //   let dept: number = 10;
  return a;
};
